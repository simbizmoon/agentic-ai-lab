# Agentic AI Lab — MASTER

## 1. 프로젝트 정의

### 프로젝트명

Agentic AI Lab

### 최종 제품명

AIRA — Agentic Intelligence Research Assistant

### 목적

Agentic AI의 핵심 이론과 개발 방법을 실제 프로젝트를 통해 학습하고,
로컬 환경에서 실용적으로 사용할 수 있는 연구 지원 도구를 완성한다.

AIRA의 핵심 사용 시나리오는 다음과 같다.

1. 사용자가 연구 질문 또는 프로젝트 문서를 제공한다.
2. AIRA가 필요한 작업을 최소한으로 계획한다.
3. 문서와 허용된 정보원을 검색하고 읽는다.
4. 근거와 출처를 연결해 결과를 작성한다.
5. 결과를 평가하고 재현 가능한 형태로 저장한다.

이 프로젝트의 성공 기준은 많은 기능을 구현하는 것이 아니라,
사용자가 직접 이해하고 운영할 수 있는 작고 신뢰할 수 있는 AIRA를
완성하는 것이다.

## 2. 최종 AIRA MVP 범위

### 필수 기능

1. 연구 요청 입력
2. 프로젝트 문서 또는 준비된 Source 검색
3. Source 읽기와 Evidence 추출
4. Claim과 Citation 연결
5. 근거 기반 보고서 생성
6. 기본 품질 평가와 Guardrail
7. 실행 상태와 결과 저장
8. 실패, 재시도 및 취소의 기본 처리
9. CLI 중심의 실제 사용 흐름
10. 선택적인 최소 FastAPI 인터페이스
11. Docker 기반 재현 가능한 실행환경
12. 사용자 가이드와 운영 메모

### 선택 기능

다음 기능은 핵심 MVP가 안정적으로 작동하고 실제 필요가 확인된 경우에만
추가한다.

- 제한된 Multi-Agent 비교 실행
- 프로젝트별 장기 Memory
- Human Approval 화면
- 간단한 Web UI
- SQLite 이후 PostgreSQL 전환
- 외부 검색 Provider 연결
- MCP, Plugins 또는 Skills 연동

### 비목표

다음 항목은 현재 프로젝트의 완료 조건이 아니다.

- 대규모 SaaS 플랫폼
- 다중 사용자 협업 제품
- Redis 또는 RabbitMQ 기반 분산 Queue
- Kubernetes
- 복수 서버 Worker Cluster
- 고가용성 Database
- 대규모 Observability Platform
- 복잡한 RBAC와 조직 관리
- 완전 자동화된 자율 연구 조직
- 상용 수준 Web UI
- 추가적인 암호화·분산 신뢰 하위 시스템

## 3. 학습 범위

### 핵심 학습

- Workflow와 Agent
- Structured Outputs
- Tool Calling
- State와 Workflow
- RAG
- Memory
- Planning
- Single Agent
- 제한된 Multi-Agent 비교
- Evals와 Guardrails
- Application Service
- Persistence와 Background Job의 기본 원리
- Docker 배포와 운영 기초

### 개발 기술

필수:

- Python 3.12
- OpenAI API와 Responses API
- Pydantic
- Git과 GitHub
- pytest와 Ruff
- Codex
- Docker

필요 시:

- FastAPI
- SQLite
- PostgreSQL
- OpenAI Agents SDK
- MCP

현재 MVP에 불필요하면 사용하지 않는다.

## 4. 핵심 설계 원칙

### 4.1 실용성 우선

새 기능은 다음 질문에 명확히 답할 수 있을 때만 추가한다.

- 실제 사용 흐름에 필요한가?
- 현재 구현보다 사용성이 분명히 좋아지는가?
- 테스트와 운영 복잡도 증가를 감당할 가치가 있는가?

세 질문 중 하나라도 불명확하면 보류한다.

### 4.2 작은 시스템부터 시작한다

일반 LLM 호출
→ 구조화 출력
→ 단일 Tool
→ Workflow
→ RAG
→ Single Research Agent
→ 평가와 Guardrail
→ 최소 Application 통합
→ 재현 가능한 배포

Planning, Memory, Multi-Agent는 이미 학습한 기능을 모두 항상 사용하는
것이 아니라, 요청에 필요한 경우에만 선택적으로 적용한다.

### 4.3 기본 실행 경로는 하나로 유지한다

AIRA의 기본 경로는 Single Research Agent이다.

Multi-Agent는 다음 조건을 모두 만족할 때만 사용한다.

- 하나의 Agent보다 역할 분리가 명확히 유리하다.
- 품질 향상이 평가로 확인된다.
- 비용과 지연 증가가 허용 범위다.

### 4.4 확정 가능한 것은 코드로 처리한다

형식 검증, 숫자 계산, 상태 전이, 권한, 중복 검사와 완료 조건은 코드로
처리한다.

요약, 분류, Evidence 해석과 보고서 작성처럼 의미 판단이 필요한 영역에
LLM을 사용한다.

### 4.5 중요한 작업은 인간이 승인한다

다음 작업은 사용자 승인 후 수행한다.

- GitHub Push
- 외부 이메일 발송
- 데이터 삭제
- 운영 서버 배포
- 운영 데이터베이스 변경
- 비용 증가 작업
- 개인정보 외부 전송
- 보안 설정 변경

### 4.6 기존 기능을 우선 재사용한다

새 Schema, Repository, Service 또는 추상화를 만들기 전에 기존 기능으로
해결 가능한지 확인한다.

### 4.7 테스트 수보다 사용자 흐름을 우선한다

테스트는 중요하지만 테스트 수 자체가 목표가 아니다.

모든 신규 테스트는 다음 중 하나를 보호해야 한다.

- 실제 사용자 흐름
- 중요한 데이터 무결성
- 재현 가능한 실패 복구
- 핵심 품질 기준

## 5. 범위 통제 규칙

새로운 Phase는 추가하지 않는다.

Phase 13 이후의 아이디어는 `Backlog`로 기록하며 자동으로 개발하지 않는다.

새 기능은 다음 절차를 거친다.

1. 실제 사용 문제 정의
2. 기존 기능으로 해결 가능한지 확인
3. 최소 구현 제안
4. 사용자 승인
5. 구현과 테스트
6. 사용 효과 검증
7. 효과가 없으면 제거 또는 동결

각 Lesson은 원칙적으로 하나의 사용자 가치 또는 하나의 핵심 통합을
완성해야 한다.

세부 Schema와 Repository를 독립 Lesson으로 계속 분리하지 않는다.

## 6. ChatGPT의 역할

### 교수

- 핵심 개념을 초보자 수준으로 설명한다.
- 세부 구현보다 전체 구조와 사용 목적을 먼저 설명한다.
- 사용자가 결과를 직접 해석하도록 돕는다.

### 시스템 설계자

- 최소 구조를 우선 제안한다.
- 기능 증가가 사용자 가치로 연결되는지 평가한다.
- 과도한 추상화와 범위 확장을 차단한다.

### Codex 감독

- 작업 범위와 금지사항을 명시한다.
- Diff, 테스트와 실제 실행 결과를 검토한다.
- 기존 코드 재사용 여부를 확인한다.

### 평가자

- 개념 이해
- 설계 판단
- 실제 사용 가능성
- 결과 품질
- 유지관리 가능성

을 함께 평가한다.

## 7. 사용자의 역할

사용자는 직접 학습하고 판단하는 개발자이며 최종 제품 사용자다.

1. 핵심 개념을 자신의 말로 설명한다.
2. 명령어와 실제 사용 흐름을 직접 실행한다.
3. 오류와 결과를 확인한다.
4. 기능이 실제로 유용한지 판단한다.
5. 불필요한 기능은 보류하거나 제거한다.
6. Codex Diff와 테스트를 검토한다.
7. 배포와 비용 증가 작업을 승인한다.

## 8. 기준 문서 우선순위

1. MASTER.md
2. DECISIONS.md
3. ROADMAP.md
4. CURRICULUM.md
5. AGENTS.md
6. README.md
7. LEARNING_LOG.md

## 9. 변경관리

- 핵심 목표를 임의로 변경하지 않는다.
- 새로운 Phase를 추가하지 않는다.
- 로드맵 변경 시 이유와 영향을 기록한다.
- 실제 필요가 없는 기능은 보류한다.
- 기존 기능을 확장하기 전에 실제 사용 검증을 한다.
- 최신 기술은 현재 MVP에 필요한 경우에만 공식 자료를 확인해 반영한다.
- 확정된 결정 변경 시 DECISIONS.md에 이력을 남긴다.

## 10. 최종 완료 조건

AIRA는 다음을 만족하면 완료한다.

1. 사용자가 CLI에서 연구 요청을 입력할 수 있다.
2. Source와 Evidence를 추적할 수 있다.
3. Claim과 Citation이 연결된 보고서를 생성한다.
4. 기본 평가와 Guardrail을 통과한다.
5. 실행 결과와 실패 상태를 저장할 수 있다.
6. 동일 요청의 중복 실행을 통제할 수 있다.
7. 핵심 흐름의 E2E 테스트가 통과한다.
8. Docker로 동일한 환경을 재현할 수 있다.
9. 실제 예제 3개 이상에서 유용성을 확인한다.
10. 사용자 가이드와 운영 메모가 존재한다.

PostgreSQL, Redis, Nginx, 분산 Worker, 고급 Web UI와 추가 Phase는
완료 조건이 아니다.
